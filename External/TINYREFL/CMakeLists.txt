set(TINYREFL_BUILD_TESTS ON CACHE BOOL)
set(TINYREFL_BUILD_EXAMPLES OFF CACHE BOOL) # Requires boost
set(TINYREFL_CROSS_BUILDING OFF CACHE BOOL)

set_directory_properties(PROPERTIES EXCLUDE_FROM_ALL YES)
FetchContent_MakeAvailable(TINTREFL)
